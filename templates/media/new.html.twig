{% extends 'base.html.twig' %}

{% block title %}Gestion des medias{% endblock %}

{% block body %}

    <h1>Adding a new Avatar</h1>

    {{ form_start(form) }}
    {{ form_row(form.type) }}
    <button class="login-btn btn-secondary" type="submit">Ajouter</button>
    {{ form_end(form) }}
{% endblock %}